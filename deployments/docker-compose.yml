services:
  catalog_db:
    image: postgres:17
    environment:
      POSTGRES_DB: catalog
      POSTGRES_USER: catalog
      POSTGRES_PASSWORD: catalog
    ports:
      - "5432:5432"
    volumes:
      - catalog_db_data:/var/lib/postgresql/data

volumes:
  catalog_db_data:
